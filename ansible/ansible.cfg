# Only picked up when running from this directory (push mode).
# ansible-pull brings its own sensible defaults.
[defaults]
interpreter_python = auto_silent
localhost_warning = false

[inventory]
inventory_unparsed_warning = false
