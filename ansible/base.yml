---
- name: Set up a unix box
  hosts: localhost
  connection: local

  roles:
    - { role: packages, tags: packages }
    - { role: system, tags: system }
    - { role: home, tags: home }
    - { role: dotfiles, tags: dotfiles }
    - { role: zen, tags: zen }
